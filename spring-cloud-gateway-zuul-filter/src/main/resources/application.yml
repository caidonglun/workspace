server.port: 8051

eureka: 
  client:
    serviceUrl:
      defaultZone: http://localhost:80/eureka/
      
spring:
  application:
    name: spring-boot-gateway-zuul-filter
    
hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds: 60000
ribbon:
  ConnectTimeout: 3000
  ReadTimeout: 60000
